<!DOCTYPE html>
<html lang="ar">

<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/images/WhatsApp Image 2025-08-24 at 6.59.04 PM.jpeg" type="image/x-icon">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Darwish_Sports</title>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #f5f5f5;
            direction: rtl;
            color: #333;
        }

        header {
            background-color: #2c7a2c;
            color: white;
            padding: 15px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        header h1 {
            font-size: 26px;
            font-weight: bold;
        }

        nav {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            padding: 6px 14px;
            border-radius: 5px;
            transition: background 0.3s;
            font-size: 16px;
        }

        nav a:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        nav a.phone {
            background-color: #f0a500;
            color: #333;
        }

        nav a.phone:hover {
            background-color: #d18e00;
        }

        main {
            flex: 1;
        }

        /* البانر */
        .banner {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background-image: url('images/banner.jpg');
            background-size: cover;
            background-position: center;
            padding: 30px 10px;
        }

        .banner h2 {
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            padding: 15px 30px;
            border-radius: 12px;
            font-size: 36px;
            margin-top: 15px;
        }

        .banner img {
            width: 100%;
            max-width: 650px;
            border-radius: 15px;
            margin: 20px 0;
        }

        .banner button {
            padding: 14px 30px;
            font-size: 18px;
            border: none;
            border-radius: 8px;
            background-color: #4CAF50;
            color: white;
            cursor: pointer;
            transition: background 0.3s, transform 0.3s;
            font-weight: bold;
        }

        .banner button:hover {
            background-color: #367636;
            transform: scale(1.05);
        }

        /* المنتجات */
        .products {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            padding: 50px 5%;
        }

        .product-card {
            background-color: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .product-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .product-card .info {
            padding: 15px;
            text-align: center;
        }

        .product-card h3 {
            margin-bottom: 10px;
            font-size: 20px;
            color: #2c7a2c;
        }

        .product-card p {
            font-size: 15px;
            color: #555;
            line-height: 1.5;
            margin-bottom: 15px;
        }

        .product-card button {
            padding: 10px 22px;
            border: none;
            background-color: #2c7a2c;
            color: white;
            border-radius: 6px;
            cursor: pointer;
            font-size: 15px;
            font-weight: bold;
            transition: background 0.3s, transform 0.3s;
        }

        .product-card button:hover {
            background-color: #1f4f1f;
            transform: scale(1.05);
        }

        /* الفوتر */
        footer {
            background-color: #2c7a2c;
            color: white;
            text-align: center;
            padding: 25px 15px;
            font-size: 16px;
        }

        footer a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
            font-size: 22px;
            padding: 10px 15px;
            border-radius: 8px;
            transition: transform 0.3s, background 0.3s;
        }

        footer a:hover {
            transform: scale(1.2);
            background-color: rgba(255, 255, 255, 0.1);
        }

        /* استجابة الشاشة */
        @media (max-width: 992px) {
            .banner h2 {
                font-size: 28px;
            }

            .banner button {
                font-size: 16px;
            }

            .product-card img {
                height: 180px;
            }
        }

        @media (max-width: 600px) {
            .banner h2 {
                font-size: 22px;
                padding: 10px 20px;
            }

            .banner button {
                font-size: 14px;
                padding: 10px 20px;
            }

            .products {
                padding: 30px 3%;
                gap: 15px;
            }

            .product-card img {
                height: 160px;
            }

            nav {
                gap: 5px;
            }

            nav a {
                font-size: 14px;
                padding: 4px 8px;
            }

            footer a {
                font-size: 20px;
                padding: 8px 12px;
            }
        }
    </style>
</head>

<body>
    <header>
        <h1>Darwish_Sports</h1>
        <nav>
            <a href="/">الرئيسية</a>
            <a href="/products">منتجاتنا</a>
            <a href="https://wa.me/201115690213" target="_blank">تواصل معنا</a>
            <a href="tel:+201115690213" class="phone">📱 اتصل بنا: 01115690213</a>
        </nav>
    </header>


    <main>
        <div class="banner">
            <button onclick="window.location.href='/products'">تسوق الآن</button>
            <img src="/images/WhatsApp Image 2025-08-24 at 6.59.04 PM.jpeg" alt="نجيله">
            <h2>حياة خضراء بدون تعب!</h2>
        </div>

        <section id="products" class="products">
            {{#each arr}}
            <div class="product-card">
                <img src="{{this.images.[0]}}" alt="{{this.productName}}">
                <div class="info">
                    <h3>{{this.productName}}</h3>
                    <p>{{this.descriptions.[0]}}</p>
                    <button onclick="window.location.href='/user/{{this._id}}'">اطلب الآن</button>
                </div>
            </div>
            {{/each}}
        </section>
    </main>

    <footer>
        &copy; 2025 جميع الحقوق محفوظة لهذا الموقع | تواصل معنا:
        <a href="https://wa.me/201032888718" target="_blank"><i class="fab fa-whatsapp"></i> واتساب</a>
        <a href="tel:+201115690213"><i class="fas fa-phone"></i> اتصل الآن</a>
    </footer>
</body>

</html>